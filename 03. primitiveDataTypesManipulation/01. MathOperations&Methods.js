const num1 = 100
const num2 = 50
let val

//Simple math w/numbers
val = num1 + num2
val = num1 - num2
val = num1 * num2
val = num1 / num2
val = num1 % num2

// Math object
val = Math.PI
val = Math.E

// Math methods
val = Math.round(2.2) // 2
val = Math.ceil(2.2) // 3
val = Math.floor(2.8) // 2
val = Math.sqrt(64) // 8
val = Math.abs(-3) // 3
val = Math.pow(8, 2) // 64
val = Math.min(2, 33, 45, 6, -1) // -1
val = Math.max(2, 33, 45, 6, -1) // 45
val = Math.random() // random number btwn 10 to 1
val = Math.floor(Math.random() * 20 + 1) 
// Int from 0 to 20


console.log(val)